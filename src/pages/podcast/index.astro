---
import PageLayout from "@/layouts/Base.astro";
import { getNavLink } from "@/lib/blog-helpers";
import { episodes } from "@/data/podcast";
---

<PageLayout
	meta={{
		title: "Podcast",
		description:
			"REWORK is a podcast about a better way to work and run your business, from 37signals — the makers of Basecamp, HEY, Fizzy, and ONCE.",
		ogImage: "/og-image/home.png",
	}}
>
	<div class="page">
		<div class="page__headline">
			<h1>Podcast</h1>
			<p>
				REWORK is a podcast about a better way to work and run your business, from 37signals — the
				makers of Basecamp, HEY, Fizzy, and ONCE.
			</p>
		</div>

		<div class="thoughts">
			<div class="thoughts__container">
				{
					episodes.map((episode) => (
						<div class="thoughts__thought">
							<h3>
								<a href={getNavLink(`/podcast/${episode.slug}/`)}>{episode.title}</a>
							</h3>
							<p class="font-size-small">{episode.date}</p>
							<p>{episode.summary}</p>
						</div>
					))
				}
			</div>
		</div>
	</div>
</PageLayout>
