---
import { getNavLink } from "@/lib/blog-helpers";

const primary: Array<[string, string]> = [
	["Signals", "/"],
	["Basecamp", "https://basecamp.com"],
	["HEY", "https://www.hey.com"],
	["Fizzy", "https://www.fizzy.do"],
	["ONCE", "https://once.com"],
];

const secondary: Array<[string, string]> = [
	["1999", "https://1999.37signals.com"],
	["Books", "/books"],
	["Thoughts", "/thoughts"],
	["Jobs", "/jobs"],
	["Updates", "https://updates.37signals.com"],
	["Podcast", "/podcast"],
	["Supply", "https://supply.37signals.com"],
	["Dev", "https://dev.37signals.com"],
	["Policies", "/policies"],
];

const isExternal = (href: string) => href.startsWith("http");
const link = (href: string) => (isExternal(href) ? href : getNavLink(href));
---

<input class="nav-active" id="nav-active" type="checkbox" />

<label class="nav-underlay" for="nav-active"></label>

<nav class="nav" aria-label="Mobile navigation">
	<div class="nav__logo">
		<a href={getNavLink("/")} aria-label="37signals">
			<img src="/assets/images/logo-black.svg" alt="37signals" />
		</a>
	</div>
	<div class="nav__toggle">
		<label for="nav-active">
			<span>Menu</span>
		</label>
	</div>
	<div class="nav__options">
		<ul>
			{
				primary.map(([label, href]) => (
					<li>
						<a
							href={link(href)}
							aria-label={label}
							target={isExternal(href) ? "_blank" : undefined}
							rel={isExternal(href) ? "noopener" : undefined}
						>
							{label}
						</a>
					</li>
				))
			}
		</ul>
		<ul>
			{
				secondary.map(([label, href]) => (
					<li>
						<a
							href={link(href)}
							aria-label={label}
							target={isExternal(href) ? "_blank" : undefined}
							rel={isExternal(href) ? "noopener" : undefined}
						>
							{label}
						</a>
					</li>
				))
			}
		</ul>
	</div>
</nav>

<header class="header">
	<h1>
		<a href={getNavLink("/")} aria-label="37signals">
			<img src="/assets/images/logo.svg" alt="37signals" />
		</a>
	</h1>
	<ul>
		{
			primary.map(([label, href]) => (
				<li>
					<a
						href={link(href)}
						aria-label={label}
						target={isExternal(href) ? "_blank" : undefined}
						rel={isExternal(href) ? "noopener" : undefined}
					>
						{label}
					</a>
				</li>
			))
		}
	</ul>
</header>
